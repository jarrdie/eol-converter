apply plugin: 'java'
apply plugin: "jacoco"

sourceCompatibility = '1.8'

repositories {
    mavenCentral()
}

dependencies {
    testCompile 'junit:junit:4.8.2'
}

compileJava {
    options.compilerArgs += ["-Xlint:unchecked"]
}