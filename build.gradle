apply plugin: 'java'
apply plugin: "jacoco"

sourceCompatibility = '1.8'

repositories {
    mavenCentral()
}

dependencies {
    testImplementation 'junit:junit:4.12'
}

compileJava {
    options.compilerArgs += ["-Xlint:unchecked"]
}